/**
 * Bump PROMPT_VERSION when buildSystemPrompt / buildUserPrompt semantics change
 * so traceability can correlate outputs with prompt generations.
 */
export const PIPELINE_VERSION = "2026.03.27";
export const PROMPT_VERSION = "care-nav-safety-feedback-1";
